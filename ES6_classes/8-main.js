import HolbertonClass from './8-holberton_class.js';

const hc = new HolbertonClass(100, 'San Francisco');
console.log(`The class size is ${Number(hc)}`);
console.log(`The class location is ${String(hc)}`);

